[tool.pytest.ini_options]
# Put the repo root on sys.path so `import molto2` works regardless of how
# pytest is invoked (plain `pytest` does not add the CWD; `python -m pytest` does).
pythonpath = ["."]
testpaths = ["tests"]
